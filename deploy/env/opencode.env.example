# OpenCode runtime environment
# Copy to: /etc/active-workbench/opencode.env

# Ensure service can find user-installed binaries (uv/opencode) if needed.
PATH=/home/active-workbench/.local/bin:/usr/local/bin:/usr/bin

# Required when exposing OpenCode over network:
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD=REPLACE_WITH_LONG_RANDOM_PASSWORD

# Optional: pin model/agent defaults for this instance.
# OPENCODE_MODEL=openai/gpt-5
# OPENCODE_AGENT=workbench-assistant

# Provider credentials (example keys; set only what your OpenCode config uses).
# OPENAI_API_KEY=REPLACE_ME
# ANTHROPIC_API_KEY=REPLACE_ME
# GROQ_API_KEY=REPLACE_ME
# FIREWORKS_API_KEY=REPLACE_ME
