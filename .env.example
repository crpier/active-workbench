# Copy to .env and fill values before running in OAuth/production mode.
# .env is already gitignored in this repository.

# OAuth runtime mode is the only supported mode.
ACTIVE_WORKBENCH_YOUTUBE_MODE=oauth
ACTIVE_WORKBENCH_SUPADATA_API_KEY=YOUR_SUPADATA_API_KEY

# Runtime data directory. OAuth files default under this directory.
ACTIVE_WORKBENCH_DATA_DIR=.active-workbench

# Optional path overrides for OAuth files.
# If unset, defaults are:
# - ${ACTIVE_WORKBENCH_DATA_DIR}/youtube-client-secret.json
# - ${ACTIVE_WORKBENCH_DATA_DIR}/youtube-token.json
# ACTIVE_WORKBENCH_YOUTUBE_CLIENT_SECRET_PATH=/absolute/path/to/youtube-client-secret.json
# ACTIVE_WORKBENCH_YOUTUBE_TOKEN_PATH=/absolute/path/to/youtube-token.json

# Optional background cadence tuning.
# ACTIVE_WORKBENCH_SCHEDULER_POLL_INTERVAL_SECONDS=60
# ACTIVE_WORKBENCH_YOUTUBE_TRANSCRIPT_SCHEDULER_POLL_INTERVAL_SECONDS=20
# ACTIVE_WORKBENCH_YOUTUBE_BACKGROUND_MIN_INTERVAL_SECONDS=180
# ACTIVE_WORKBENCH_YOUTUBE_TRANSCRIPT_BACKGROUND_MIN_INTERVAL_SECONDS=20

# Optional: bucket metadata enrichment provider key.
# ACTIVE_WORKBENCH_BUCKET_OMDB_API_KEY=YOUR_OMDB_API_KEY

# Optional: lightweight internal telemetry (local structured logs only).
# ACTIVE_WORKBENCH_TELEMETRY_ENABLED=true
# ACTIVE_WORKBENCH_TELEMETRY_SINK=log
